# ============================================================
# GitHub Actions: Deploy to Cloudflare Pages
# 【トリガー】GitHubのmainブランチにpushされたとき
# 【動作】
#   1. Next.jsをビルド（npm run build）
#   2. Cloudflare Pagesにデプロイ
#   ※ GitLab逆同期は現在無効化
# ============================================================

name: Deploy to Cloudflare Pages

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      deployments: write

    steps:
      # -------------------------------------------------------
      # 1. コードを取得
      # -------------------------------------------------------
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # -------------------------------------------------------
      # 2. Node.js セットアップ
      # -------------------------------------------------------
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      # -------------------------------------------------------
      # 3. 依存パッケージのインストール
      # -------------------------------------------------------
      - name: Install dependencies
        run: |
          if [ -f package-lock.json ]; then
            npm ci --no-audit
          else
            npm install --no-audit
          fi

      # -------------------------------------------------------
      # 4. カラー変換スクリプト（存在する場合のみ実行）
      # -------------------------------------------------------
      - name: Convert colors (if script exists)
        run: |
          if [ -f scripts/convert-colors.js ]; then
            npm run convert-colors
          else
            echo "convert-colors script not found - skipping"
          fi

      # -------------------------------------------------------
      # 5. Next.js ビルド
      # -------------------------------------------------------
      - name: Build
        run: npm run build

      # -------------------------------------------------------
      # 6. CMS用adminフォルダをコピー
      # -------------------------------------------------------
      - name: Copy admin folder to build output
        run: |
          echo "📁 Copying admin folder to build output..."
          if [ -d "public/admin" ]; then
            mkdir -p out/admin
            cp -r public/admin/* out/admin/
            echo "✅ Admin folder copied successfully!"
            echo "📋 Files in out/admin:"
            ls -la out/admin/
          else
            echo "⚠️ Warning: public/admin folder not found - skipping"
          fi


      # -------------------------------------------------------
      # 6. Cloudflare Pages にデプロイ
      # -------------------------------------------------------
      - name: Deploy to Cloudflare Pages
        uses: cloudflare/pages-action@v1
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          projectName: ${{ secrets.CLOUDFLARE_PROJECT_NAME }}
          directory: out
          gitHubToken: ${{ secrets.GITHUB_TOKEN }}

      # -------------------------------------------------------
      # 7. GitLabに逆同期（現在無効化）
      # -------------------------------------------------------
      # - name: Sync back to GitLab
      #   env:
      #     GITLAB_TOKEN: ${{ secrets.GITLAB_TOKEN }}
      #     GITLAB_REPO: ${{ secrets.GITLAB_REPO }}
      #   run: |
      #     echo "===== Syncing back to GitLab ====="
      #
      #     git config --global user.name "GitHub Actions"
      #     git config --global user.email "actions@github.com"
      #
      #     git remote remove gitlab 2>/dev/null || true
      #     git remote add gitlab https://oauth2:${GITLAB_TOKEN}@gitlab.com/${GITLAB_REPO}.git
      #
      #     echo "Pushing to GitLab..."
      #     git push gitlab main:main --force
      #
      #     echo "===== Sync to GitLab completed ====="
