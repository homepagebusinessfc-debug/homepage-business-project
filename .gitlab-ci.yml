image: node:18

stages:
  - build
  - deploy

cache:
  paths:
    - node_modules/
    - .next/

variables:
  NODE_ENV: production

build:
  stage: build
  script:
    - npm install  # npm ci から変更
    - npm run build
  artifacts:
    paths:
      - out/
      - .next/
    expire_in: 1 hour
  only:
    - main
    - develop

pages:
  stage: deploy
  dependencies:
    - build
  script:
    - mkdir -p public
    - cp -r out/* public/
  artifacts:
    paths:
      - public
  only:
    - main